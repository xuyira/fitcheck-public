# FitCheck 分阶段开发与验收文档

## 0. 项目目标与范围

FitCheck 是一个以 AI 试衣为核心的简历项目。用户可以不登录完成试衣和下载；只有保存到衣橱或添加到日历时才需要登录。

### 首版确定范围

- 试衣默认使用“一张人物图 + 一张服装图”的整套换装流程。
- 首版先接入阿里云百炼 `aitryon`，效果评测后可切换 `aitryon-plus`。
- 暂不实现只换上衣、只换下装的复杂分割链路；页面可以预留按钮，但首版先只启用“换整套”。
- 背景为可选上传项；不上传时默认保留人物照片中的原背景。
- 游客可以上传、生成、查看、下载。
- 保存到衣橱、添加到日历时弹出登录框。
- 添加到日历会自动保存到衣橱。
- 衣橱保存的是“穿搭作品”。AI 作品保存最终图、人物原图、服装原图和可选背景原图；本地上传作品只保存最终图。
- 衣橱是图片相册；详情页显示最终图和可用的原始图；支持删除、下载、加入日历。
- 日历显示已加入日期的穿搭图；支持从衣橱选择或本地上传；删除日历记录不删除衣橱作品。
- 用户只能访问自己的衣橱、日历和图片。
- 暂不做短信登录、微信登录、支付、社交分享、推荐算法和复杂服装元数据。

## 1. 你已准备好的外部资源

### 百炼

- 阿里云百炼 API Key，地域为华北2（北京）。
- 开发阶段可以将模型设置为 `aitryon`；效果确认后切换 `aitryon-plus`。

### OSS

- 普通对象存储 OSS Bucket，不是向量 Bucket。
- 地域为华北2（北京），标准存储，本地冗余，私有读写。
- 已创建只允许操作该 Bucket 的 RAM 用户和 AccessKey。
- 前端直传所需的 CORS 规则。

### 本地环境

- Node.js LTS
- Docker Desktop（用于本地 PostgreSQL）
- Git

真实密钥只写入项目根目录 `.env.local`，不发送到聊天、不提交 Git。项目中只提交 `.env.example`。

## 2. 总体技术方案

- 前端与后端：Next.js + TypeScript
- UI：Tailwind CSS + Lucide 图标
- 数据库：PostgreSQL + Prisma
- 登录：邮箱 + 密码，服务端会话
- 图片：OSS 私有 Bucket；数据库只保存 Object Key，不保存公开永久链接
- AI：百炼异步试衣接口；后端轮询任务并将结果复制到 OSS
- 部署：Docker + 阿里云 ECS；数据库初期可与应用同机，数据库端口不向公网开放
- Provider 结构：预留 `bailian`、`doubao`、`jimeng` 三个模型提供方，首版只实现百炼

## 3. 分阶段开发流程

每个阶段完成后暂停，由你按验收清单检查。你可以选择：

1. 通过：进入下一阶段。
2. 需要修改：指出具体页面、文案、交互或规则，我只修改当前阶段范围。
3. 暂停：保留当前代码和运行方式，下一次继续。

除非你明确说“进入下一阶段”，我不会默认扩展到后续阶段。

---

## 阶段 1：产品骨架与视觉原型

### 我完成

- 初始化 Next.js 项目、基础布局、路由和导航。
- 实现试衣页、结果页、衣橱页、衣橱详情页、日历页、登录弹窗的静态交互。
- 使用本地占位图片或渐变占位，不调用真实 API。
- 完成移动端和桌面端响应式布局。
- 确定背景选择面板：默认 / 上传 / 选择，分类为室内、城市、自然、景区。
- 试衣页默认选中“换整套”；上衣/下装按钮可显示为“后续支持”或暂不展示，避免造成可用性误解。

### 你验收

- 启动开发服务器后访问 `http://localhost:3000`。
- 能从试衣页进入结果、衣橱、详情和日历。
- 检查手机宽度和桌面宽度下是否有遮挡、溢出或按钮难以点击。
- 检查上传区域、背景卡片、相册卡片和详情页是否符合预期。

### 验收问题

- 视觉风格是否满意？
- 试衣页步骤是否清楚？
- 背景分类和卡片提示是否自然？
- 衣橱相册密度、悬浮按钮和详情布局是否需要调整？

### 阶段产物

- 可点击的前端原型。
- 页面截图。
- 暂不包含真实登录、数据库和 AI。

---

## 阶段 2A：工程化迁移（已完成，待用户验收）

### 实施内容

- 将阶段 1 的 Vite 单文件原型迁移为 Next.js App Router + TypeScript。
- 页面路由拆分为 `/try`、`/wardrobe`、`/calendar`。
- 组件拆分为布局、试衣流程、结果页、衣橱、日历与弹窗。
- 保留阶段 1 已确认的视觉、响应式布局和静态交互。
- 当前仍使用演示数据，不接数据库、OSS 和百炼。

### 验收内容

- 三个路由均可直接访问和刷新。
- 试衣三步、上传预览、结果页、衣橱详情和日历切换可用。
- 手机底部导航与桌面布局无明显回退。
- `npm run typecheck` 与 `npm run build` 均通过。

---

## 阶段 2B：登录、数据库与用户隔离（已完成，待用户验收）

> 本地环境未安装 PostgreSQL/Docker，因此阶段 2B 使用 Prisma + SQLite 完成开发与隔离验收。部署前将 Prisma datasource 切换为 PostgreSQL；业务接口和用户归属逻辑不变。

### 我完成

- 建立 PostgreSQL 和 Prisma schema。
- 实现注册、登录、退出和会话保持。
- 完成用户密码哈希，禁止明文存储。
- 建立 `users`、`sessions`、`outfits`、`calendar_entries`、`try_on_tasks`、`backgrounds` 表。
- 实现衣橱和日历的增删查接口。
- 所有接口从服务端会话取得 `user_id`，不信任前端传入的用户 ID。
- 为单条资源读取、删除、下载和日历关联增加归属检查。
- 游客点击保存或加入日历时，弹出登录框；登录成功后继续原操作。

### 你验收

- 注册两个测试账号：A 和 B。
- A 创建作品、加入日历、删除作品。
- B 登录后确认看不到 A 的作品和日历。
- 修改请求中的资源 ID，确认不能读取或删除另一用户的资源。
- 未登录访问衣橱和日历接口时得到登录要求。

### 阶段产物

- 本地可运行的多用户业务骨架。
- 数据库迁移文件和 ER 图。
- 安全测试记录。

---

## 阶段 3：OSS 图片链路

### 我完成

- 配置 OSS SDK 和服务端环境变量。
- 实现服务端生成临时上传凭证或受控上传接口。
- 实现人物图、服装图、背景图和本地作品上传。
- Bucket 保持私有；图片展示使用短期签名 URL。
- 增加文件大小、格式和基础尺寸检查。
- 删除作品时同步删除其 OSS 对象。
- 增加临时任务和结果图的过期字段，准备后续清理任务。
- 配置开发域名 CORS；正式域名确定后再增加正式来源。

### 你验收

- 上传一张人物图、一张服装图和一张背景图。
- 刷新页面后图片仍可显示。
- 复制图片 URL 到无权限环境，确认不能长期直接访问。
- 删除作品后，数据库记录和 OSS 对象均被删除。
- 上传超大文件、错误格式文件，确认有明确提示。

### 阶段产物

- 私有 OSS 的完整图片生命周期。
- `.env.example` 和部署配置说明。

---

## 阶段 4：百炼整套试衣

### 我完成

- 实现百炼 Provider，默认整套换装。
- 将人物图和服装图转换为百炼可访问的 HTTPS 地址。
- 创建异步任务，保存 provider task ID。
- 轮询任务状态：排队、处理中、成功、失败。
- 成功后立即下载百炼结果并保存到自己的 OSS，避免24小时结果地址失效。
- 前端显示生成进度、失败原因和重试入口。
- 游客可以生成和下载；保存或加入日历时要求登录。
- 记录模型名、耗时、失败状态和 request ID，便于后续评测。

### 你验收

- 使用合规的正面单人全身人物图和服装平铺图生成一次结果。
- 确认结果页可以预览和下载。
- 未登录点击保存到衣橱，登录后作品成功保存。
- 未登录点击添加到日历，登录后选择日期，确认作品同时进入衣橱和日历。
- 测试一张不合规图片，确认不会卡死并显示可理解的错误。
- 检查百炼控制台用量和项目日志中没有泄露 API Key。

### 阶段产物

- 第一条真实 AI 试衣链路。
- 百炼调用说明和错误码处理说明。

---

## 阶段 5：背景处理

### 我完成

- 实现默认背景：直接使用百炼结果。
- 实现上传背景：保存背景图并合成最终图。
- 实现预设背景库和搜索框。
- 背景分类：室内、城市、自然、景区。
- 背景卡片使用空的发光人物轮廓提示人物将放入场景。
- 初版使用人像分割/蒙版和前端或服务端合成，不增加生成式背景模型。
- 支持人物缩放、位置、边缘羽化和简单阴影参数。

### 你验收

- 分别测试默认、上传和预设背景。
- 检查人物边缘、比例、位置和背景方向。
- 搜索“海边”“图书馆”“长城”等关键词，确认分类和搜索结果正确。
- 检查默认背景不会额外产生不必要的处理等待。

### 阶段产物

- 完整背景选择与合成链路。
- 可扩展的背景数据结构。

---

## 阶段 6：完整验收与部署

### 我完成

- Docker 化应用和数据库。
- 配置 Nginx、HTTPS、日志和健康检查。
- 部署到阿里云 ECS。
- 安全组只开放必要端口；数据库不开放公网端口。
- 配置域名（备案完成后）或提供临时公网 IP 访问。
- 增加清理临时图片和过期任务的定时任务。
- 提供演示账号、README、架构图、数据库图和 API 文档。

### 你验收

- 招聘者可以在公网打开网站。
- 注册、登录、试衣、下载、保存衣橱、添加日历、删除全部可用。
- 两个账号数据完全隔离。
- 手机和桌面端均可完成主流程。
- 重启容器后数据库和已保存作品仍存在。
- 查看 README 是否足以说明项目亮点和运行方式。

### 阶段产物

- 可公开演示的网站。
- 简历项目材料和演示视频建议稿。

---

## 4. 模型评测阶段（部署完成后）

首版不阻塞产品开发。使用同一组固定图片比较：

- 百炼 `aitryon`
- 百炼 `aitryon-plus`
- 百炼 Plus + `aitryon-refiner`（如果成本和额度允许）
- 豆包 Provider（若取得正式可用 API）
- 即梦 Provider（若取得正式可用 API）

建议指标：

- 人脸一致性
- 服装款式一致性
- 颜色与纹理还原
- Logo/文字还原
- 人体结构和手部质量
- 背景保持能力
- 成功率
- 平均耗时
- 单张成本
- 人工综合评分

后端统一记录 `provider`、`model`、`task_id`、`latency_ms`、`status` 和评分字段，后续接入其他模型不需要重做页面和数据库主流程。

## 5. 你每阶段给我的反馈格式

验收时可以直接回复：

```text
阶段：1
结果：通过 / 需要修改
设备：手机 / 桌面 / 两者
问题：页面或交互问题
希望：具体修改方式
```

例如：

```text
阶段：1
结果：需要修改
问题：背景选择面板太高，手机上需要滚动两次才能看到确定按钮
希望：改成底部抽屉，底部固定确定按钮
```

## 6. 第一轮行动

下一步从阶段 1 开始。我会先搭建可点击的网页原型，不接真实百炼和 OSS。完成后启动本地开发服务器，并给出访问地址和阶段 1 验收清单。你确认阶段 1 后，再进入登录和数据库。
